import { Router } from "express";

export const adminRouter = Router();

adminRouter.post("/element", (req, res) => {});

adminRouter.put("/element/:id", (req, res) => {});

adminRouter.post("/avatar", (req, res) => {});
